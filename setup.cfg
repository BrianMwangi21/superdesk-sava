[flake8]
max-line-length = 120
exclude = env,bin,lib,include,client,build,dist,node_modules,.git,.mypy_cache
# E203/W503/W504 conflict with black's formatting; ignore for compatibility.
ignore = E203, W503, W504
